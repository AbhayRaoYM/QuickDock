# QuickDock Sidecar Server — environment variables
# Copy to .env and fill in values.

NODE_ENV=development

# Sidecar listen address
QUICKDOCK_HOST=127.0.0.1
QUICKDOCK_PORT=3579

# Directory where quickdock.db will be created
QUICKDOCK_DB_DIR=.data
